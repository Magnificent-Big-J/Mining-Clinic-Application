<h2 class="table-avatar">
    <a href="{{route('home')}}" class="avatar avatar-sm mr-2">
        <img class="avatar-img" src="{{asset($row->image_path)}}" alt="Speciality">
    </a>
    <a href="{{route('home')}}">{{$row->name}}</a>
</h2>
